{
  "name": "@eve-os/utils",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "uuid": "^14.0.1"
  },
  "devDependencies": {
    "@eve-os/config": "workspace:*",
    "@types/uuid": "^11.0.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
